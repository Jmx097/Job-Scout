# ===========================================
# Job Scout Backend - Production Environment
# ===========================================

# Database (SQLite for MVP - stored in Railway volume)
DATABASE_URL=sqlite+aiosqlite:///./data/jobscout.db

# Encryption Key (REQUIRED - generate a new key for production)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_production_fernet_key

# Clerk Backend
# Get these from clerk.com -> Your App -> Production -> API Keys
CLERK_SECRET_KEY=sk_live_...
CLERK_ISSUER=https://your-app.clerk.accounts.dev

# CORS - Lock to production frontend domain only (JSON array format)
# Update this with your actual Vercel deployment URL
CORS_ORIGINS=["https://your-app.vercel.app"]

# JobSpy Proxy (optional - helps avoid rate limits in production)
JOBSPY_PROXY_URL=

# OpenAI Key TTL (hours before requiring re-entry)
OPENAI_KEY_TTL_HOURS=24
